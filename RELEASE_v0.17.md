# v0.17 — SVG本編統合・買いそろえる経済

## SVG

- `art/cat-directions` の試作v6を、本編の全猫表示へ統合。
- 試作ページ自体は変更しない。`scripts/build-cat-runtime.cjs` で共通描画部分を機械抽出し、`assets/cat-svg.js` を生成する。
- 保護器を開けたときに見た目を抽選し、`cat.phenotype` に保存。描画、移動、タイプシャッフル、リロードでは再抽選しない。
- 36柄、頭・胴・脚・尾の柄、顔・耳・目・髭・尾の形、子猫／成猫。柄の出現重みは試作のものを継承。
- 未保護猫には耳カットを付けず、TNR完了時に追加。三毛・錆柄の新規個体は暫定でメス。
- 部屋では歩行、ジャンプ、玩具への反応、食事、ごろごろ／ふみふみ、休息へ姿勢を接続。保護器内でも性格によって異なる姿勢。寝姿などの自律選択は仮調整。
- 移動経路・ジャンプの軌道は従来の本編制御が担当し、SVGのジャンプ移動を重ねない。歩行速度と素早いジャンプは維持。
- 子猫の卒業判定は子猫用体重目標700gと5日以上、成猫は従来3000gを使用。すべてゲーム内の仮数値であり、医療判断ではない。

## 初期セット

新規ゲーム：50コイン、保護器1種、段ボール／ごはん皿／水入れ／トイレ、ネズミ1個、ごはん3皿分、掃除ペーパー6回分。水は無料。

初期から毛布・棚・タワー・複数玩具・大量の消耗品は持たせない。

## ショップ

- 27商品：玩具7、家具10、ごはん5、掃除／ふれあい用品5。
- 新規：ひらひらリボン、虫のじゃらし、香り袋、かくれネズミ、くぐりトンネル、ひなたマット、壁のハンモック、チキンごはん、香るスープ。既存のぬいぐるみも配置可能に。
- 道具の相性は1タイプのみ。「いつもの」「はじめて」を道具に持たせない方針を維持。
- スーパーとペットショップで購入。部屋・受け入れ中も、押し入れの「＋ 買う」から買い足せる。画面遷移で受け入れ状態を失わない。
- 家具は所持したものだけ押し入れに表示。同一家具は1個、壊れたら買い替え可。未所持家具を配置する更新も拒否する。
- 玩具は複数購入可。最後の1個が壊れた後、購入すると耐久を戻して再使用可能。
- 価格・購入単位は `SHOP_CATALOG` が正。画面から送られた金額を信用せず、商品表から取得する。

## コイン設計（仮）

| 内容 | コイン |
|---|---:|
| 初期活動費 | 50 |
| 広告ダミー1回 | +50 |
| 初診・ワクチン（7＋5） | 12 |
| 手術・卒業ケア（送り出す時） | 18 |
| ごはん6皿分 | 4〜6 |
| 掃除用品12〜24回分 | 3〜5 |
| ふれあい用品12回分 | 3〜4 |
| 玩具1個 | 4〜7 |
| 家具1個 | 3〜12 |

1匹につき50〜100コインを目標。初診・卒業30、ごはん8〜18、掃除／ふれあい9〜20、玩具4〜7で**51〜75コイン**程度。初回家具を最大23コイン分買い足す想定でも**74〜98コイン**程度。

これは買い物モデルの目安で、支出上限ではない。長い飼育、過剰な補充、全商品収集などでは超える。再利用家具・初期支給を使う場合は下回る。時間進行は従来の高速モックのままであり、正式な一周の収支・継続率は今後のプレイ検証で調整する。

全136通りの単／複タイプで、初期家具とショップ家具から受け入れ可能な構成が存在することを確認。最安構成の買い足し費用は最大23コイン（初診後38コイン以内）。

広告は3.2秒のダミー。完了時のみ50コイン、中止・リロードは0。古い完了通知・二重通知を拒否。実広告SDK・通信・課金・バックエンド検証は未実装。

## 既存セーブ

- 初回移行時のみ、旧所持金を36で割って四捨五入（旧1800→50）。旧所持品と配置は残す。旧版では全家具が利用可能だったため、その家具は引き続き所有扱い。
- 猫の名前・進行を維持し、既存個体にも固定のSVG見た目を割り当てる。
- 新しい初期体験を試す場合はタイトルの「初期セットから遊び直す」。確認ダイアログ付き。前のセーブを別キーへバックアップしてから新規開始する。
- セーブ・通貨はブラウザ内のデータ。改ざん耐性はない。本番の広告報酬には別途サーバー側検証が必要。

## 検証・配布

- `tests/v017.test.cjs`：新規通しプレイ、全タイプ受け入れ費用探索、ショップ、広告完了／中止、金額偽装、未所持配置拒否、保存、譲渡／TNR、スマホ幅。
- 既存の操作回帰テストは「購入済み」のフィクスチャを明記し、初期支給の変更から分離。
- 保護された配布用ビルドは外部ゲームスクリプトを暗号化前に埋め込み、引き続き単一HTMLにする。
- 本変更はローカル実装。GitHub Pagesへの更新はまだ行っていない。
