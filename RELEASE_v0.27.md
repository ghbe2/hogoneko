# v0.27 実時間と不在計算

- realClock.processedAt を計算済み日時、dayTicks を日内の3時間刻みとして保存。保存操作の savedAt と分離し、3時間未満の端数を保持。24時間で日数・曜日・心の上限・体重履歴が進む（午前0時での日付切替ではない）。
- 起動・操作・30秒の確認タイマー・タブ復帰・pagehideで同じ時計を同期。旧30秒高速シミュレーションと20秒不在換算を無効化。3日なら24刻みを順に計算。56刻みの切り捨てはしない。
- 食事・水・消化・排泄・抜け毛を既存の生活計算で進める。家具は2日ごとに対象1個の耐久を1減らす。食料・水は器に残っている分だけ消費し、かばんからは自動補充しない。
- 仮バランス：空腹（満腹度25未満）、水分25未満、トイレ3以上または嘔吐汚れ、の各状態が24時間を超えて続くと、該当項目ごと3時間あたり心−0.1。空腹継続時は成猫−5g／子猫−1g。心の既存下限0は維持。医学的な数値ではなくゲーム用の仮値。
- 予定日の誘導を計算完了後に適用。誘導中でも実時間は止めない。手術・譲渡は自動完了しない。延期は同じ日に帰宅し、翌日再度誘導。
- 通常の予定表から翌日スキップを削除。デバッグ＋3時間は壁時計の日時を未来にせず、シミュレーションだけを進める。NEXT_DAYはテスト・デバッグ用として残す。
- 旧セーブは読み込んだ元のsavedAtを基点に移行（初期化中の自動保存で上書きされたsavedAtは使わない）。旧セーブに3時間未満の端数記録はないため移行時は0から。受け入れ開始・次の保護では時計をリセット。
- 端末時刻の巻き戻しではマイナス経過や重複計算をせず、時計が計算済み日時に追いつくまで待つ。端末時計ベース・ブラウザ内保存で、サーバー時刻や端末間同期は未実装。
- tests/v027.test.cjs: 72時間、備蓄の消費、放置損失、逐次計算との一致、端数、時計巻き戻し、30日、再読み込みの一度だけ計算、デバッグと旧タイマー遮断。
- ローカル実装。公開未更新。
