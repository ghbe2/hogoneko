# v0.25 メニューのちらつき修正

- 原因：反応が残っている間の全アクションで消去タイマーを再予約し、CLEAR_REACTION（期限切れも含む）が全画面を再構築。開いているシートの登場アニメーションも毎回再生されていた。
- 反応IDが新しくなったときだけタイマーを登録。期限切れIDは即時終了。有効な消去も反応DOMと猫の一時クラスだけ更新し、メニューDOMは保持。
- かばん・さわり方・呼び方は可視データが変わらなければ再描画しない。背後のシミュレーションは継続。
- 表示内容が変わる更新では在庫等を反映し、開くアニメーションは再生しない。同じメニュー内のスクロール・入力値・フォーカス・選択範囲を復元。別タブ／閉じて開き直す際は通常表示。
- tests/v025.test.cjs：実タイマー待ちでDOM保持、重複予約なし、在庫更新、開き直し、スクロール位置、入力／カーソル、手帳を検証。
