# v0.22 こする操作

- 掃除スロットから道具を持ち、汚れの上で往復すると連続で掃除。ドロップ・静止では処理しない。従来の汚れタップによる掃除は無効。
- 1掃除分の移動距離は `48 / sqrt(power)` px、作用範囲は汚れの外縁から `12 + power * 12` px。瞬間移動や小さいポインター振動による過剰処理を抑制。
- 同じ範囲の複数対象は1回分で処理。有限道具は実際に掃除できたときだけ1回消費。無限ぞうきんは消費なし。汚れの薄まり・範囲の点線・掃除のきらめきでフィードバック。
- かばんから「さわる」を除去。別のさわり方メニューは撫でる・コーム・ウェットの3つのみ。
- ネコの上をこすると選択中のケア。650ms動かさず長押しすると抱く（既存の心・好み判定あり）。移動で抱っこの予約をキャンセル。抱いているネコを押すと下ろす。
- tests/v022.test.cjs で掃除の連続操作・静止・消費・メニュー・長押しとこすりの判別を検証。
