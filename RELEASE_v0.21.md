# v0.21 操作スロット・初期用品

- あそぶ／ごはん／そうじは58pxの丸を8px間隔で集約。道具名の重複表示を廃止し、絵・残数・カテゴリだけ表示。道具名は選択画面とアクセシブル名で確認できる。
- 丸の上に△。ドラッグ中も同じSVGを表示。
- よぶ／さわるは押下から600msで背景が満ちる。短押し実行・長押し変更の既存操作を維持。移動・キャンセル時は演出解除。
- 毛は6種類の小さい束。IDから形・大きさ・向きを決めるため再描画でちらつかない。掃除対象の当たり判定は28pxを維持。
- 無限の初期用品：布きれじゃらし（育つ速度50%）、おすそわけカリカリ（25補充）、小さなぞうきん（強さ0.5、毛は1束）、小さな水くみ（既存の水を25補充に変更）。連続補充は50。
- 新規開始時は初期用品をセット。進行中のセーブは所持品・選択を保持し、初期用品を追加。JSONにInfinityは保存しない。購入品の耐久・在庫消費は従来通り。

検証：tests/v021.test.cjs、tests/v019.test.cjs、tests/v020.test.cjs。375pxスクリーンショットを目視確認、320/375/430pxで横はみ出しなし。
